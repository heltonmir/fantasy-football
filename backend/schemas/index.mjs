"use strict";
import commonSchema from "./common.mjs";
import playerSchema from "./player.mjs";
import userTeamSchema from "./userTeam.mjs";

const schemas = [commonSchema, playerSchema, userTeamSchema];

export default schemas;
